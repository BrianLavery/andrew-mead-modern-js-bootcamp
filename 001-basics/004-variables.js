// 1. Can't declare a variable more than once
let petName = 'Hal';
petName = 'spot'

console.log(petName);

// 2. There are rules related to variable names

// This would be invalid
// let 3 = 2;
// let result = 3 + 4

// a. Variables can only contain letters, _ or $
// Can contain but not begin with numbers
// b. Cannot be reserved keywords (e.g. let)
